version: '3.8'

services:
  whatsapp-gateway:
    build: .
    container_name: whatsapp-gateway
    restart: always
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
      - BOT_SECRET=your_secret_key_here
    volumes:
      - ./auth_info:/app/auth_info
